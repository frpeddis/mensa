services:
  - type: web
    name: mensa-angelini
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: TZ
        value: Europe/Rome
      - key: ANTHROPIC_API_KEY
        sync: false
